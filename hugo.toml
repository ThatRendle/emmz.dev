baseURL = 'https://emmz.dev/'
locale = 'en-gb'
title = 'Emmz Rendle'
theme = 'ink-console'
enableRobotsTXT = true

# Blog sections that feed the homepage + RSS
[params]
  author = 'Emmz'
  description = 'Notes on Claude Code, agentic workflows, and building software with AI — by Emmz Rendle.'
  eyebrow = 'writing · claude code · agentic dev'
  tagline = 'Professional Agent Wrangler'
  logoText = 'emmz.dev'
  footerNote = 'Built with Hugo · Ink Console'
  mainSections = ['posts']
  consolePath = 'posts'
  aboutURL = '/about/'
  # image = 'images/og-default.jpg'   # optional default Open Graph image (place in static/images/)

  # End-of-post call-out. Remove `url` to hide it everywhere;
  # set `noCta = true` in a post's front matter to hide it on that post.
  [params.workshop]
    url = 'https://claude.rendle.dev'
    eyebrow = 'workshop'
    title = 'Want to do this stuff yourself?'
    blurb = 'Effective Claude Code is my hands-on workshop covering the agentic workflows I write about here — the practices, the tooling, and the habits that make Claude Code actually productive.'
    cta = 'Book the workshop'

[pagination]
  pagerSize = 10

[taxonomies]
  tag = 'tags'

[menu]
  [[menu.main]]
    name = 'Posts'
    url = '/posts/'
    weight = 10
  [[menu.main]]
    name = 'Tags'
    url = '/tags/'
    weight = 20
  [[menu.main]]
    name = 'About'
    url = '/about/'
    weight = 30

  [[menu.footer]]
    name = 'GitHub'
    url = 'https://github.com/ThatRendle'
    weight = 10
    [menu.footer.params]
      external = true
  [[menu.footer]]
    name = 'LinkedIn'
    url = 'https://linkedin.com/in/rendledotdev'
    weight = 20
    [menu.footer.params]
      external = true
  [[menu.footer]]
    name = 'RSS'
    url = '/index.xml'
    weight = 30

[markup]
  [markup.highlight]
    noClasses = true
    style = 'nord'
    tabWidth = 2
  [markup.goldmark.renderer]
    unsafe = true
